import { RenderMorphemViewer, DownloadSvg, DownloadPng } from "./assets/js/app";

if (window) {
  window.RenderMorphemViewer = RenderMorphemViewer;
  window.DownloadSvg = DownloadSvg;
  window.DownloadPng = DownloadPng;
}
